apiVersion: v1
kind: ServiceAccount
metadata:
  name: mlflow-sa
  annotations:
    # Replaced per-environment by overlays/<env>/patch-service-account-arns.yaml
    # (the real value comes from `terraform output mlflow_iam_role_arn`).
    eks.amazonaws.com/role-arn: "REPLACED_BY_OVERLAY"
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-sa
  annotations:
    eks.amazonaws.com/role-arn: "REPLACED_BY_OVERLAY"
